# library(pastecs)

# library(foreign)
# ld <- read.csv('ts.csv')

# nums <- sapply(ld, is.numeric)

# ldNum <- ld[,nums]

# test <- ldNum[sample(1:nrow(ld), 500, replace=FALSE),]

# corm <- cor(test, use= 'pairwise.complete.obs', method='spearman')

# test2 <- ldNum[sample(1:nrow(ld), 5000, replace=FALSE),]

# corm2 <- cor(test2, use= 'pairwise.complete.obs', method='spearman')


# corm3 <- cor(ldNum, use= 'pairwise.complete.obs', method='spearman')


